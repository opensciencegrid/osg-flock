
COLLECTOR_DEBUG = $(COLLECTOR_DEBUG) D_AUDIT D_SECURITY D_FULLDEBUG
MAX_COLLECTOR_LOG = 256 MB

# note - this is currently not including D_SECURITY, so revocation does not show up
COLLECTOR_AUDIT_LOG = $(LOG)/AuditLog

